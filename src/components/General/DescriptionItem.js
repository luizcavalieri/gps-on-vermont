import React from 'react';

const DescriptionItem = ({ item }) => {
    // console.log(item);
    return (
        <>
            <div>{item}</div>
        </>
    );
}

export default DescriptionItem;